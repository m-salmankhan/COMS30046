;=================================================
;==== Add 1 to number labeled by input ===========
;=================================================

; load input arg into r0
ADDI r0 r0 0x9

; add 1 to input and store in r1
ADDI r1 r0 1
HALT